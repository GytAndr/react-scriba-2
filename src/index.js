import ReactDOM from "react-dom";
import "./normalize.css"; //to normalize defoult browser stylings
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(<App />);
